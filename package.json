{
  "scripts": {
    "build": "./scripts/compile.sh",
    "watch": "npm run build && chokidar 'packages/**/src/**/*.js' -c 'npm run build'"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-1": "^6.5.0",
    "chokidar-cli": "^1.2.0",
    "lerna": "2.0.0-beta.26"
  },
  "dependencies": {
    "babel-loader": "^6.2.4"
  }
}
